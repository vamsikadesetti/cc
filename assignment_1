#include <stdio.h>

int cube(int n);

main()
{
    int n, result;
    
    printf("Enter a number: ");
    scanf("%d", &n);
    
    result = cube(n);
    
    printf("The cube of %d is %d", n, result);
}

int cube(int n)
{
    return n * n * n;
}
